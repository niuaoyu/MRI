
- (i) 把"生成式的、以形状为条件的 3D 建模"问题，形式化为学习一个**连续隐式曲面（SDF）**；什么是"生成式的、以形状为条件的 3D 建模"问题，我理解的生成式是指扩散模型，生成本不存在的东西，是这个生成式吗？以形状为条件的 3D 建模什么意思？还有以什么别的条件3D建模吗？
- (ii) 提出一种基于概率自解码器（auto-decoder）的训练方法——不用编码器（encoder），直接对每个训练形状学习一个隐向量； 这里不用编码器为什么可以学习隐向量？之前编码解码为什么不行？这里的隐向量从哪里得到的？
- (iii) 把这套方法应用到形状建模和**形状补全（shape completion）**任务上，并取得当时的最好效果。  形状建模指的是什么？是说学习形状的分布，然后得到的 补全又指的是什么？