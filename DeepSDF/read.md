
- (i) 把"生成式的、以形状为条件的 3D 建模"问题，形式化为学习一个**连续隐式曲面（SDF）**；什么是"生成式的、以形状为条件的 3D 建模"问题，我理解的生成式是指扩散模型，生成本不存在的w
- (ii) 提出一种基于**概率自解码器（auto-decoder）**的训练方法——不用编码器（encoder），直接对每个训练形状学习一个隐向量；
- (iii) 把这套方法应用到形状建模和**形状补全（shape completion）**任务上，并取得当时的最好效果。